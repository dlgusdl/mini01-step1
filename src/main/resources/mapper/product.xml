<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="site.metacoding.firstapp.model.ProductRepository">

 <select id="findAll" resultType="site.metacoding.firstapp.model.Product">
    select * from product
</select>

  <select id = "findById" resultType = "site.metacoding.firstapp.model.Product">
        select * from product where product_id  = #{productId}
    </select>

    <select id="findByName" resultType="site.metacoding.firstapp.model.Product">
    select * from product where product_name = #{productName}
  </select>

  <insert id = "insert">
        insert into product (product_name, product_price, product_qty, created_at) values (#{productName}, #{productPrice}, #{productQty}, now())
    </insert>

  <update id = "update">
        update product set 
        product_name = #{productName}, 
        product_price = #{productPrice}, 
        product_qty = #{productQty} where product_id  = #{productId}
    </update>

  <delete id = "deleteById">
        delete from product where product_id = #{productId}
    </delete>

</mapper>

